#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b, c;
    cin >> a >> b >> c;
    int ans = a + b + c;
    cout << "Sum of " << a << ", " << b << " and " << c << " is " << ans << endl;
}