/*#include<stdio.h>
int main(){
    FILE *file;
    file=fopen("shuvo.txt", "w");
    char str[100];gets(str);
    fputs(str, file);
    fclose(file);
    return 0;
}*/
/*#include <stdio.h>
int main()
{
    FILE *file;
    file = fopen("shuvovai.c", "r");
    char ch;
    ch = fgetc(file);
    while (!feof(file))
    {
        printf("%c", ch);
        ch = fgetc(file);
    }
    fclose(file);
}*/
